program -> statement* EOF;

statement -> expressionStatement
			| printStatement
			| declarationStatement
			| if
			| "\n";

expressionStatement -> expression "\n"?;
printStatement -> "print" "(" expression ")" "\n"?;
declarationStatement -> TYPE IDENTIFIER ("=" expression)? "\n"?;
if -> "if" expression ":" "\n" block
	("elif" expression ":" "\n" block)? 
	("else" expression ":" "\n" block)?;
while -> "while" expression ":" "\n" block;
for -> "for" TYPE IDENTIFIER "in" "range" "(" INT ("," INT){0,2} ")" ":" "\n" block;

block -> INDENT{1,} statement* (OUTDENT{1,})?; // Indent/outdent count must match

expression -> assignment;

assignment -> IDENTIFIER "=" ternary
			| ternary;

ternary -> binLogic "if" binLogic "else" binLogic;

binLogic -> negated (("and" | "or" | "xor" | "&&" | "||" | "^^" | "nand" | "nor" | "xnor") negated)*;

negated -> ("not" | "!") negated
		| comp;

comp -> conversion (("==" | "!=" | ">" | "<" | ">=" | "<=") conversion)*;

conversion -> bitwise ("as" TYPE)*;

bitwise -> term (("<<" | ">>" | "&" | "|" | "^") term)*;

term -> factor (("+" | "-") factor)*;

factor -> unary (("/" | "*" | "%") unary)*;

exp -> factor ("**" factor)*;

unary -> ("-" | "~") unary
		| primary;

primary -> NUM | STRING | BOOL | NULL
		| "(" expression ")"
		| IDENTIFIER;
